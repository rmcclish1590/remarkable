//! SFTP push/pull transfer primitives used by the sync engine.

/// Transfer operations (pull from device, push to device).
pub struct Transfer;

impl Transfer {
    /// Construct a transfer handle. Implemented in specs 12/13.
    pub fn new() -> Self {
        todo!()
    }
}
