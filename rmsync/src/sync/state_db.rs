//! SQLite-backed sync state database.

/// Handle to the sync-state SQLite database.
pub struct StateDb;

impl StateDb {
    /// Open (or create) the state database. Implemented in spec 05.
    pub fn open() -> Self {
        todo!()
    }
}
