//! Three-state diff and sync orchestration across local, remote, and DB state.

/// Sync engine entry point. Implemented across specs 11–14.
pub struct Engine;

impl Engine {
    /// Construct the sync engine. Implemented in spec 11.
    pub fn new() -> Self {
        todo!()
    }
}
