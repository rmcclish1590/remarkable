//! GtkApplication subclass and top-level window wiring.

/// Placeholder for the application-level controller that will own the main
/// window, shared state, and glue between UI and sync subsystems.
pub struct App;

impl App {
    /// Construct the application controller. Implemented in a later spec.
    pub fn new() -> Self {
        todo!()
    }
}
