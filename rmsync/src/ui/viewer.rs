//! Document viewer panel — renders rendered SVG pages for the selected notebook.

/// Viewer widget. Single-page in spec 20, multi-page scrollable in spec 21.
pub struct Viewer;

impl Viewer {
    /// Build the viewer widget. Implemented in spec 20.
    pub fn new() -> Self {
        todo!()
    }
}
