//! Sync button, progress bar, and sync-folder selector.

/// Container widget grouping the user-facing sync controls.
pub struct SyncControls;

impl SyncControls {
    /// Build the sync controls widget. Implemented in spec 17/19.
    pub fn new() -> Self {
        todo!()
    }
}
