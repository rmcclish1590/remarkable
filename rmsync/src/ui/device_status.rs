//! Device connection status indicator (connected / disconnected / error).

/// Small indicator widget reflecting the current device state.
pub struct DeviceStatus;

impl DeviceStatus {
    /// Build the status indicator widget. Implemented in spec 18.
    pub fn new() -> Self {
        todo!()
    }
}
