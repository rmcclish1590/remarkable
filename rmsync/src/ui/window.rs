//! Main application window (root of the widget tree).

/// Top-level window widget. Composed of the folder browser, viewer, and
/// sync controls.
pub struct MainWindow;

impl MainWindow {
    /// Build the main window. Implemented in spec 15.
    pub fn new() -> Self {
        todo!()
    }
}
