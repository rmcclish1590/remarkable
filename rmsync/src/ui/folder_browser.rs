//! Document tree sidebar showing the reMarkable folder hierarchy.

/// Sidebar widget displaying notebooks and folders as a tree.
pub struct FolderBrowser;

impl FolderBrowser {
    /// Build the folder browser widget. Implemented in spec 16.
    pub fn new() -> Self {
        todo!()
    }
}
