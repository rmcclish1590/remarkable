//! SSH/SFTP session management for the reMarkable tablet.

/// A live SSH connection to the device.
pub struct Connection;

impl Connection {
    /// Open an SSH connection to the device. Implemented in spec 06.
    pub fn open() -> Self {
        todo!()
    }
}
