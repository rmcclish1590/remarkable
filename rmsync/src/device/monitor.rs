//! udev-based monitor that detects reMarkable tablets connecting via USB.

/// USB device monitor. Implemented in spec 07.
pub struct Monitor;

impl Monitor {
    /// Start watching udev events. Implemented in spec 07.
    pub fn start() -> Self {
        todo!()
    }
}
