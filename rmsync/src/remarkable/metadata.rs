//! Parser for reMarkable `.metadata` JSON sidecar files.

/// Parsed metadata for a single notebook or folder. Implemented in spec 02.
pub struct Metadata;

impl Metadata {
    /// Parse a `.metadata` file's JSON contents. Implemented in spec 02.
    pub fn parse() -> Self {
        todo!()
    }
}
