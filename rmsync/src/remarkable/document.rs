//! Document model — the tree of notebooks and folders synced from the device.

/// Root document model. Implemented in later specs alongside metadata parsing.
pub struct Document;

impl Document {
    /// Construct an empty document tree. Implemented in spec 02+.
    pub fn new() -> Self {
        todo!()
    }
}
