//! Parser for the reMarkable `.rm` v6 binary stroke format.

/// Parsed stroke data for a single page. Implemented in spec 03.
pub struct RmPage;

impl RmPage {
    /// Parse raw `.rm` bytes into stroke data. Implemented in spec 03.
    pub fn parse() -> Self {
        todo!()
    }
}
