//! Converts parsed stroke data into rendered SVG for display.

/// SVG renderer for reMarkable stroke data. Implemented in spec 04.
pub struct SvgRenderer;

impl SvgRenderer {
    /// Render a page's strokes to SVG. Implemented in spec 04.
    pub fn render() -> Self {
        todo!()
    }
}
